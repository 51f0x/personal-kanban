# Frontend Environment Variables
# These are used by Vite and prefixed with VITE_ to be exposed to the client

# API Base URL
# Default: http://localhost:3000/api/v1
VITE_API_URL=http://localhost:3000/api/v1

# WebSocket URL for realtime updates
# Default: http://localhost:3000
VITE_WS_URL=http://localhost:3000

# Capture Token (optional)
# If set, this token will be included in capture requests
# Should match CAPTURE_ACCESS_TOKEN from the API .env file
VITE_CAPTURE_TOKEN=

